<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Civic Issue Reporting</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: url('/images/uploads/backImg.jpg') no-repeat center center fixed;
      background-size: 100% 100%;  /* Image fits all sides, no crop, no zoom */
      background-color: #2196F3;   /* Fallback color if image fails */
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
      overflow: hidden;
    }

    /* Existing fancy gradient/floating particles removed for real photo background */

    .login-container {
      position: relative;
      z-index: 1;
      background: #1f500329;
      backdrop-filter: blur(60px);
      -webkit-backdrop-filter: blur(20px);
      padding: 50px 40px;
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2),
        0 0 0 1px rgba(14, 87, 44, 0.3) inset;
      width: 100%;
      max-width: 470px;
      text-align: center;
      animation: slideIn 0.6s ease-out;
      border: 1px solid rgba(255, 255, 255, 0.3);
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    h2 {
      margin-bottom: 2rem;
      color: #ffffff;
      font-size: 2rem;
      font-weight: 700;
      text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.2);
      letter-spacing: 0.5px;
      animation: fadeIn 1s ease-out 0.2s both;
    }
    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    .input-wrapper {
      position: relative;
      margin-bottom: 25px;
      animation: fadeIn 1s ease-out 0.4s both;
    }
    input[type="email"],
    input[type="password"] {
      width: 100%;
      padding: 15px 20px;
      border: 2px solid rgba(255, 255, 255, 0.3);
      border-radius: 12px;
      font-size: 1rem;
      background: rgba(255, 255, 255, 0.2);
      color: #1f0909;
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      outline: none;
      font-weight: 500;
    }
    input[type="email"]::placeholder,
    input[type="password"]::placeholder {
      color: rgba(10, 49, 2, 0.507);
      font-weight: 400;
    }
    input[type="email"]:focus,
    input[type="password"]:focus {
      border-color: rgba(255, 255, 255, 0.8);
      background: rgba(255, 255, 255, 0.25);
      box-shadow: 0 0 20px rgba(255, 255, 255, 0.3),
        0 0 0 4px rgba(255, 255, 255, 0.1);
      transform: translateY(-2px);
    }
    .input-wrapper::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      width: 0;
      height: 2px;
      background: linear-gradient(90deg, #4CAF50, #2196F3);
      transition: all 0.4s ease;
      transform: translateX(-50%);
    }
    .input-wrapper:focus-within::after {
      width: 100%;
    }
    .btn-wrapper {
      animation: fadeIn 1s ease-out 0.6s both;
    }
    input[type="submit"] {
      width: 100%;
      padding: 15px;
      background: linear-gradient(135deg, #4CAF50, #45a049);
      color: white;
      font-size: 1.15rem;
      font-weight: 600;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      margin-top: 10px;
      box-shadow: 0 4px 15px rgba(76, 175, 80, 0.4);
      position: relative;
      overflow: hidden;
      letter-spacing: 0.5px;
    }
    input[type="submit"]::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
      transition: left 0.6s ease;
    }
    input[type="submit"]:hover::before {
      left: 100%;
    }
    input[type="submit"]:hover {
      background: linear-gradient(135deg, #45a049, #4CAF50);
      transform: translateY(-3px);
      box-shadow: 0 6px 25px rgba(76, 175, 80, 0.5);
    }
    input[type="submit"]:active {
      transform: translateY(-1px);
      box-shadow: 0 3px 15px rgba(76, 175, 80, 0.4);
    }
    p {
      margin-top: 25px;
      font-size: 1rem;
      color: rgba(255, 255, 255, 0.9);
      font-weight: 500;
      animation: fadeIn 1s ease-out 0.8s both;
    }
    a {
      color: #3dbd67;
      text-decoration: none;
      font-weight: 700;
      transition: all 0.3s ease;
      position: relative;
      padding-bottom: 2px;
    }
    a::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 0;
      height: 2px;
      background: #ffffff;
      transition: width 0.3s ease;
    }
    a:hover::after {
      width: 100%;
    }
    a:hover {
      text-shadow: 0 0 10px rgba(255, 255, 255, 0.8);
    }
    @media(max-width: 480px) {
      .login-container {
        padding: 40px 30px;
        max-width: 90%;
      }
      h2 {
        font-size: 1.6rem;
      }
      input[type="email"],
      input[type="password"],
      input[type="submit"] {
        font-size: 0.95rem;
        padding: 12px 16px;
      }
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h2>Login to Your Account</h2>
    <form action="/login" method="post">
      <div class="input-wrapper">
        <input type="email" name="email" placeholder="Enter your email" required>
      </div>
      <div class="input-wrapper">
        <input type="password" name="password" placeholder="Enter your password" required>
      </div>
      <div class="btn-wrapper">
        <input type="submit" value="Login">
      </div>
    </form>
    <p>Don't have an account? <a href="/register">Register here</a></p>
  </div>
</body>
</html>
