<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Live Photo Required</title>

  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      background: linear-gradient(135deg, #fff, #e8f5e9);
      padding: 30px;
    }

    h2 {
      color: #c62828;
      font-size: 28px;
      margin-bottom: 10px;
    }

    p {
      font-size: 16px;
      margin-bottom: 20px;
    }

    video {
      width: 340px;
      max-width: 90%;
      border-radius: 12px;
      border: 3px solid #2e7d32;
      box-shadow: 0 5px 12px rgba(0,0,0,0.2);
    }

    button.captureBtn {
      margin-top: 20px;
      padding: 12px 20px;
      background: linear-gradient(135deg, #2e7d32, #43a047);
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      box-shadow: 0 4px 10px rgba(46,125,50,0.3);
    }

    button.captureBtn:hover {
      transform: scale(1.05);
    }

    button.submitBtn {
      padding: 12px 22px;
      background: linear-gradient(135deg, #1b5e20, #4caf50);
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 18px;
      cursor: pointer;
      opacity: 0.6;
      margin-top: 25px;
      box-shadow: 0 4px 10px rgba(46,125,50,0.3);
    }

    button.submitBtn.enabled {
      opacity: 1;
      cursor: pointer;
    }

    .footer {
      margin-top: 40px;
      color: #555;
      font-size: 14px;
    }
  </style>
</head>

<body>

  <h2>⚠️ Sorry to hear that!</h2>
  <p>Please capture a <b>live photo</b> showing that the issue is still unresolved.</p>

  <!-- Camera feed -->
  <video id="camera" autoplay playsinline></video>

  <br>

  <!-- Capture button -->
  <button class="captureBtn" onclick="capturePhoto()">📸 Capture Live Photo</button>

  <canvas id="canvas" style="display:none;"></canvas>

  <!-- Upload Form -->
  <form id="uploadForm" action="/admin/issue/<%= customId %>/uploadFeedback" method="POST">
    <input type="hidden" name="userFeedbackImage" id="photo">
    
    <button id="submitBtn" class="submitBtn" disabled>
      Upload Live Photo
    </button>
  </form>

  <script>
    const video = document.getElementById("camera");
    const canvas = document.getElementById("canvas");
    const photoInput = document.getElementById("photo");
    const submitBtn = document.getElementById("submitBtn");

    // Start camera
    navigator.mediaDevices.getUserMedia({ video: true })
      .then(stream => video.srcObject = stream)
      .catch(() => alert("Camera access required!"));

    // Capture Function
    function capturePhoto() {
      canvas.width = video.videoWidth;
      canvas.height = video.videoHeight;

      canvas.getContext("2d").drawImage(video, 0, 0);

      const imgData = canvas.toDataURL("image/jpeg");
      photoInput.value = imgData;

      // Enable button
      submitBtn.disabled = false;
      submitBtn.classList.add("enabled");

      alert("✅ Live photo captured successfully!");
      
      // Auto-hide capture page after success
      setTimeout(() => {
        submitBtn.scrollIntoView({ behavior: "smooth" });
      }, 700);
    }
  </script>

  <div class="footer">Powered by Civic Portal</div>

</body>
</html>
