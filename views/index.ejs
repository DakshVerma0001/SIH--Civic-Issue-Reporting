<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CivicFix – Report & Resolve Civic Issues</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    * { margin:0; padding:0; box-sizing:border-box; }
    body { font-family:'Poppins',sans-serif; background: #f0f4f8; }

    /* Navbar */
    .navbar {
      position: fixed;
      top:0; left:0; right:0;
      height:70px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:0 50px;
      background: linear-gradient(135deg, rgba(24,90,157,0.95), rgba(67,206,162,0.85));
      backdrop-filter: blur(10px);
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
      z-index:100;
      border-bottom: 1px solid rgba(255,255,255,0.2);
    }
    .navbar h2 { 
      color:#fff; 
      font-size:1.8rem; 
      font-weight:700;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
      letter-spacing: 1px;
    }
    .navbar-links {
      display: flex;
      gap: 15px;
    }
    .navbar a {
      color:#fff;
      text-decoration:none;
      font-weight:600;
      padding: 10px 25px;
      border-radius: 25px;
      transition: all 0.3s ease;
      background: rgba(255,255,255,0.1);
      border: 2px solid rgba(255,255,255,0.3);
    }
    .navbar a:hover { 
      background: rgba(255,255,255,0.25);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }

    /* Hero */
    .hero {
      position: relative;
      height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      text-align:center;
      color:#fff;
      overflow:hidden;
    }
    .hero img {
      position:absolute;
      width:100%;
      height:100%;
      object-fit:cover;
      z-index:-2;
      filter: brightness(0.7);
    }
    .hero::after {
      content:"";
      position:absolute;
      inset:0;
      background: linear-gradient(135deg, rgba(67,206,162,0.6), rgba(24,90,157,0.7));
      z-index:-1;
    }
    .hero-content { 
      max-width: 800px; 
      padding:0 20px;
      animation: fadeInUp 1s ease-out;
    }
    .hero h1 { 
      font-size:3.5rem; 
      font-weight:700; 
      margin-bottom:1.5rem; 
      line-height:1.2;
      text-shadow: 3px 3px 6px rgba(0,0,0,0.3);
      letter-spacing: 1px;
    }
    .hero p { 
      font-size:1.35rem; 
      line-height:1.8;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Features */
    .features {
      background: linear-gradient(135deg, #f0f4f8 0%, #e8f1f5 100%);
      padding:100px 40px;
      display:grid;
      grid-template-columns: repeat(auto-fit,minmax(320px,1fr));
      gap:40px;
      max-width: 1400px;
      margin: 0 auto;
    }
    
    .card {
      background: linear-gradient(145deg, #ffffff, #f8fbff);
      border-radius:20px;
      padding:40px 30px;
      text-align:center;
      box-shadow: 0 8px 30px rgba(24,90,157,0.15);
      transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      border: 2px solid rgba(67,206,162,0.2);
      position: relative;
      overflow: hidden;
    }
    
    .card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, rgba(67,206,162,0.1), rgba(24,90,157,0.1));
      opacity: 0;
      transition: opacity 0.4s ease;
      z-index: 0;
    }
    
    .card:hover::before {
      opacity: 1;
    }
    
    .card:hover { 
      transform:translateY(-12px) scale(1.02);
      box-shadow: 0 15px 40px rgba(24,90,157,0.25), 
                  0 0 0 4px rgba(67,206,162,0.3);
      border-color: rgba(67,206,162,0.5);
    }
    
    .card-icon-wrapper {
      position: relative;
      z-index: 1;
      width: 90px;
      height: 90px;
      margin: 0 auto 25px;
      background: linear-gradient(135deg, #43cea2, #185a9d);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 6px 20px rgba(24,90,157,0.3);
      transition: all 0.4s ease;
    }
    
    .card:hover .card-icon-wrapper {
      transform: rotateY(360deg) scale(1.1);
      box-shadow: 0 8px 25px rgba(67,206,162,0.5);
    }
    
    .card img { 
      width:55px; 
      height: 55px;
      filter: brightness(0) invert(1);
    }
    
    .card h3 { 
      position: relative;
      z-index: 1;
      margin-bottom:15px; 
      color:#185a9d;
      font-size: 1.5rem;
      font-weight: 700;
      letter-spacing: 0.5px;
    }
    
    .card p {
      position: relative;
      z-index: 1;
      color: #555;
      line-height: 1.7;
      font-size: 1rem;
    }

    /* Footer */
    footer {
      text-align:center;
      padding:50px 30px;
      background: linear-gradient(135deg, #185a9d, #0e3c6b);
      color:#fff;
      font-size: 1rem;
      position: relative;
      overflow: hidden;
      box-shadow: 0 -5px 20px rgba(0,0,0,0.15);
    }
    
    footer::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, #43cea2, #185a9d, #43cea2);
    }
    
    footer p {
      font-weight: 500;
      letter-spacing: 0.5px;
      text-shadow: 1px 1px 3px rgba(0,0,0,0.2);
      margin: 0;
    }

    @media(max-width:768px){
      .navbar { padding: 0 20px; }
      .navbar h2 { font-size: 1.4rem; }
      .navbar a { padding: 8px 18px; font-size: 0.9rem; }
      .hero h1 { font-size:2.2rem; }
      .hero p { font-size:1.1rem; }
      .features { padding: 60px 20px; gap: 30px; }
      .card { padding: 35px 25px; }
    }

    @media(max-width:480px){
      .navbar-links { gap: 8px; }
      .navbar a { padding: 6px 12px; font-size: 0.85rem; }
      .hero h1 { font-size: 1.8rem; }
      .hero p { font-size: 1rem; }
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar">
    <div>
      <h2>CivicFix</h2>
    </div>
    
    <div class="navbar-links">
      <a href="/register">Register</a>
      <a href="/login">Login</a>
    </div>
  </nav>

  <!-- Hero -->
  <section class="hero">
    <img src="https://www.eqimg.com/images/2024/1920x1080/09022024-image1-equitymaster.jpg" alt="city view">
    <div class="hero-content">
      <h1>Report, Track & Resolve Civic Issues</h1>
      <p>Join your community in making cities cleaner, safer, and smarter with real-time civic engagement.</p>
    </div>
  </section>

  <!-- Features -->
  <section class="features">
    <div class="card">
      <div class="card-icon-wrapper">
        <img src="https://img.icons8.com/fluency/96/000000/camera.png" alt="">
      </div>
      <h3>Smart Reporting</h3>
      <p>Quickly report issues with photos, videos, and live GPS to help authorities take immediate action.</p>
    </div>
    <div class="card">
      <div class="card-icon-wrapper">
        <img src="https://img.icons8.com/fluency/96/000000/group.png" alt="">
      </div>
      <h3>Collaborative Platform</h3>
      <p>Work together with neighbors to verify, support, and prioritize issues for maximum community impact.</p>
    </div>
    <div class="card">
      <div class="card-icon-wrapper">
        <img src="https://img.icons8.com/fluency/96/000000/marker.png" alt="">
      </div>
      <h3>Real-Time Tracking</h3>
      <p>Stay updated with live notifications as reported issues get resolved step by step.</p>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <p>© 2025 CivicFix • Building smarter, responsive, and sustainable cities</p>
  </footer>

</body>
</html>